#1. Escriba un programa en Python donde el usuario in
# grese un string por teclado y luego imprima todas sus letras (una en cada fila). Realíce una versión iterando sobre las letras del string y otra iterando sobre los índices de las letras (es decir, usando for-range).
# Ej: si el string ingresado es auto deberá escribir:
string_entrante = input('Ingrese su string')
for i in string_entrante:
    print(i)